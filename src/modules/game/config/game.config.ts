export const GameConfig = {
	heroRecruitment: {
		defaultMaxRecruits: 3, //The default number of recruits to spawn in a location if it's not specified on the tile,
		defaultRefreshTime: 30 * 60000 //The default interval (in milliseconds) to check if a location should add more recruits
	}
};